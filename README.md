# DDPM-with-Optimal-Transport-Theory
This is an experiment that integrates DDPM with Optimal Transport Theory. It has four models, including a baseline model that uses solely DDPM in training and sampling,  a model that uses DDPM + Sinkhorn loss regularity term in training and baseline sampling, a model that uses DDPM in training and Sinkhorn distance gradient guidance in sampling.
